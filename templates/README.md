# Templates

Reusable planning and agent templates live in repo-local template folders:

- `00_agile/templates/` for filesystem Kanban task, suggestion, and architecture proposal cards
- `.github/ISSUE_TEMPLATE/` when GitHub issue forms are added

Add StarRupture runtime or compose templates here only when they are reusable beyond the current deployment manifests.
